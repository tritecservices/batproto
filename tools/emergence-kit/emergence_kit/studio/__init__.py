"""Survey Studio: the Emergence Review Kit as a desktop app for ecologists.

The window is a web page served by a small local server (``server.py``) that calls the
same engine as the command line. On Windows it opens as its own app window through
Microsoft Edge (built into Windows 10 and 11), with no browser tabs or address bar.
"""
